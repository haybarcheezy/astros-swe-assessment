services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: baseball-stats-backend
    ports:
      - "5001:5001"
    environment:
      - FLASK_ENV=development
      - FLASK_DEBUG=1
      - DATABASE_URL=sqlite:///data/baseball.db
    volumes:
      - ./backend:/app/:cached

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: baseball-stats-frontend
    ports:
      - "3000:3000"
    environment:
      # Vite dev-server proxy target: the backend service via Docker DNS.
      # (Replaces VITE_API_URL, which made the browser call the backend
      # directly and bypass the /api proxy, relying on CORS.)
      - BACKEND_URL=http://backend:5001
    volumes:
      # 1. Mount entire monorepo for editing
      - .:/workspace:cached

      # 2. Specific frontend mounts for hot-reload at /app (where Vite runs)
      - ./frontend/src:/app/src/:cached
      - ./frontend/public:/app/public/:cached

      # 3. Shadow volumes - prevent host from overwriting container dependencies
      - /workspace/frontend/node_modules
      - /workspace/backend/.venv
    depends_on:
      - backend
